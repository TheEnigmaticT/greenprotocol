export const calculatePurificationBurden = (protocol: Record<string, unknown>) => {
  // Logic to estimate wash-step count and isolation friction
  void protocol // placeholder — real logic lives in Python chemistry service
  return { score: 0.5, metrics: { washes: 2, isolations: 1 } };
};
