# GreenChemistry.ai Kanban Reclamation Report — 2026-07-02

## Ground truth checked
- Live board DB: `~/.hermes/kanban/boards/green-chemistry-ai/kanban.db`
- Pre-reset snapshot metadata: `~/.hermes/kanban/snapshots/greenchem-pre-reset-20260701_235217/metadata.json`
- Night crew runner/logs and writeback reports
- Markdown source-of-truth file: `/Users/ct-mac-mini/dev/greenchemistry-ai/BACKLOG.md`
- Obsidian sync config: `~/.hermes/scripts/obsidian_kanban_sync.yaml`

## Key finding
The current markdown source of truth is effectively empty. `BACKLOG.md` now contains only section headers. But the most recent shift-state snapshot still says the board had **89 linked markdown cards** at shift start and **1,386 known Hermes tasks**. So reclamation must be driven from **(a) the empty current markdown board** plus **(b) the last linked-card snapshot**, not from the live Hermes board alone.

## Current board shape
- Total tasks: 1,481
- Statuses:
  - todo: 736
  - ready: 463
  - done: 186
  - blocked: 62
  - archived: 34
- Assignee/status concentration:
  - orchestrator: 309 ready / 261 todo / 61 blocked
  - reviewer: 9 ready / 345 todo / 1 blocked
  - worker-gemma: 145 ready / 130 todo
- Zero-run backlog:
  - orchestrator ready: 306/309 have never run
  - orchestrator todo: 254/261 have never run
  - reviewer todo: 345/345 have never run
  - worker-gemma ready: 145/145 have never run
  - worker-gemma todo: 130/130 have never run

## Source-of-truth mismatch
- Current `BACKLOG.md`: **0 live cards**
- Shift-start snapshot for green-chemistry-ai: **89 linked markdown cards**
- 2026-07-01 writeback reports show `status: conflict` for green-chemistry-ai because markdown changed after shift start.
- Latest kanban sync report before the reset saw:
  - `card_count: 89`
  - `orphan_hermes_tasks: 1297`

Interpretation: almost the entire Hermes board is now orphaned relative to markdown, and even before the markdown file was emptied, only 89 tasks were still anchored to markdown.

## Reclamation buckets
### 1) Survive as canonical carry-forward work
Use the 89 shift-start linked tasks as the only automatic survivor set. Everything else should have to justify its existence.

### 2) Canonicalize
Keep blocked roots that explicitly name a canonical path and use them only as references while preserving one real child lane.
Typical examples:
- roots waiting on a documented canonical child path
- blocked roots with a precise `done-when` and one bounded child chain

### 3) Archive / quarantine
Strong default for:
- duplicate / superseded / quarantine cards
- reviewer fan-in cards with no surviving parent lane
- implementation/spec leaves generated by decomposition churn but not tied to a surviving markdown card
- orphan Hermes tasks that were never represented in current markdown

### 4) Regenerate cleanly from markdown
After reclamation, rebuild only the intentionally chosen work back into markdown and let sync recreate the board from that smaller set.

## Quantitative recommendation
### Human intervention
Blocked cards break down roughly as:
- explicit human-gate blockers: 15
- duplicate/quarantine blockers: 27
- technical/canonical-wait blockers: 20

So the true "needs human intervention" count is closer to **15**, not all 62 blocked tasks.

### Unfixed / unreclaimed
If current markdown is the source of truth, then:
- **0** current markdown cards are cleanly represented in `BACKLOG.md` now
- **89** prior linked cards are the maximum candidate survivor pool from the shift-state snapshot
- **1,297** Hermes tasks were already orphaned versus markdown in the last sync report
- **1,392** tasks are still active (`todo + ready + blocked`)

Operationally, the board is still **almost entirely unreclaimed**.

## Concrete board reclamation move set
1. Freeze automation (already done / paused).
2. Treat current `BACKLOG.md` as authoritative emptiness unless Trevor chooses to restore cards into it.
3. Extract the 89 shift-start linked tasks into a temporary survivor worksheet.
4. For each survivor, keep only one canonical lane:
   - root reference card if useful
   - one active implementation/spec/review path at most
5. Bulk archive everything matching duplicate/superseded/quarantine patterns.
6. Bulk archive reviewer TODO cards that have no surviving upstream path.
7. Bulk archive orphan tasks not in the 89-card survivor worksheet.
8. Rebuild `BACKLOG.md` with only the deliberately retained work.
9. Re-run sync from markdown to recreate a much smaller clean board.

## Why the orchestrator must be fixed before re-enabling automation
The board shape shows the orchestrator compiled work into the wrong lanes:
- orchestrator still owns many execution leaves (`Implement`, `Draft`, `Write`)
- worker-gemma still owns many spec/audit leaves (`Define`, `Audit`, `Design`)
- reviewer was used as a sink, creating 345 zero-run TODO review cards

Without fixing routing and duplicate suppression first, a rebuild from markdown will just re-inflate the same mess.
