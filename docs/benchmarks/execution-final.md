# Historical execution checkpoint — superseded by active resumption

Executor has acknowledged BENCHMARK-ACCESS-APPROVAL.md. The old authorization and historical-spend blockers below are no longer current. Status is RUNNING with approved bounded metadata discovery awaiting its ordered review, real role adapters/provider/stage storage in implementation, and no real model calls yet. The $100 ledger is sprint-cumulative, excluding historical experiments without resetting sprint usage. See execution-status.json and execution-log.md for current gates. This document retains earlier failure evidence and is not the final mission result.

## Result

The requested all-procedure Qwen/Gemma end-to-end migration has NOT been executed or completed. No real corpus was loaded, no application inference ran, and no real case received substantive acceptance. This is not qualification READY, a completed first sprint, scientific parity, local-device proof, or application integration.

A metadata-only check of the specifically documented original research input was denied by the single-query command approval gate before execution. That access path was stopped without alternate access. Consequently the complete procedure cohort, duplicate and historical-split counts, previous Qwen exact IDs, approved frozen evidence/baselines, and cumulative historical OpenRouter spend remain unknown. Independent loader reviews do not clear the access denial.

## Preserved boundary

Execution worktree: `/Users/ct-mac-mini/dev/greenchemistry-ai-local-e2e`
Branch: `research/local-model-e2e-20260905`
HEAD: `30f252000a86ade611e688291d40eec399edf541`, matches freshly fetched origin/main.

Original research worktree remains at `b4f1d0ff211562a500167083fd0894809412d1fa`, with exactly its four original modified files. Their restricted binary diff still hashes to `8b75e7c3fceae6b478b2610af64dad7ab552601a9897bb63babbe976ab05afec`.

All execution changes remain uncommitted. No push, deployment, main/production modification, production database write, protocol publication, or dependency installation was performed. The shared node_modules symlink was reused.

## Implemented software, not real chemistry results

- Generic immutable UTF-8 source/offset/hash graph, exact anchors, byte accounting, closed claim validation and separate independent-inventory reconciliation primitives. Full typed chemistry facts and actual extraction/review/repair orchestration remain unimplemented.
- All twelve declared principle contracts and explicit unavailable job status; orthogonal durable decisions and locked-evidence applicability gate. Independent specification findings require follow-up verification; declarations are not working principle jobs.
- Occurrence-specific draft patch and scenario scoring callback substrate. Real graph-derived material completeness, conversion provenance, deterministic chemistry scorer integration and user acceptance/application are not implemented.
- Strict mockable OpenRouter transport and append-only budget/attempt substrate, with immutable request gates, bounded full-body deadlines, serial reservation, raw synthetic failure archives and durable stop after uncertain cancellation. Live construction remains `LIVE_NOT_APPROVED`.
- Isolated Python helper routing guard before legacy provider priority; explicit yield, SMILES, P8 and P11 roles. This is not a live transport bridge or end-to-end helper qualification. Existing legacy routing outside isolation is retained.
- Read-only rooted corpus loader with synthetic verification. It returns protocol plus minimal counts, not a complete evidence/baseline execution corpus. No actual loader run occurred.

## Model and spend status

Only `google/gemma-4-31b-it` was matched to prior safe source and a current official model listing (https://openrouter.ai/google/gemma-4-31b-it). Previous hosted Qwen235B/27B IDs remain unknown; no guessed IDs were enabled.

Read-only `ollama list` rechecked the parent inventory: `hf.co/bartowski/google_gemma-4-31B-it-GGUF:Q5_K_M` (displayed ID `444acd94dfeb`, 23 GB) and `hf.co/unsloth/Qwen3.8-27B-GGUF:Q4_K_M` (displayed ID `603ce507f0fd`, 18 GB). Neither was invoked. Inventory is not model-role qualification, full checkpoint attestation, local capacity, or hosted/local equivalence evidence.

New application inference calls: 0. New OpenRouter spend: $0. Historical and cumulative spend: UNKNOWN, not zero. New Anthropic calls: 0. New OpenAI embedding calls: 0. No baseline regeneration.

Accounting checkpoint: `/Users/ct-mac-mini/dev/local-model-migration-planning/mission-spend-ledger.json`. Runtime paid ledger is not initialized: unknown historical spend cannot be replaced by a zero-dollar balance. Reconcile every prior billed retry/audit/failure before provisioning the one cumulative runtime ledger.

## Verification and independent reviews

Baseline restored benchmark: 6 files / 25 tests passed, typecheck and targeted lint passed.
Latest stable-subset parent check: `npm test -- tests/lib/local-qualification/source-graph.test.ts tests/lib/local-qualification/coverage-audit.test.ts tests/lib/local-qualification/corpus.test.ts tests/lib/local-qualification/provider-manifests.test.ts` — 4 files / 93 tests passed.
Python parent check: `python3.14 -m pytest -q -p no:cacheprovider test_isolated_llm_boundary.py test_llm_client.py` — 6 passed.

Full final verification and review states are recorded in execution-status.json when the remaining corrective lanes settle. These intermediate counts must not be substituted for final whole-scope acceptance.

Parent whole-suite run at 22:24:52: `npm test -- --reporter=dot` returned 39 files / 485 tests passed. Its legacy pipeline logs contain Sonnet labels, but `tests/lib/pipeline-ranking.test.ts:9–13` and `tests/lib/pipeline-partial-scoring.test.ts:11–15` replace the Anthropic SDK with `MockAnthropic`; their chemistry and literature clients are also mocked. Those logs are not real model calls or regenerated baselines. A subsequent concurrent typecheck saw one in-progress graph-test typing diagnostic, so this intermediate run is not the final frozen-tree verification.

Python and corpus units have independent narrow spec PASS and quality APPROVED artifacts. Graph has narrow spec PASS but explicit full T1/T2 gaps. Contracts and patch spec findings were returned to corrective TDD lanes. Runtime recovery preserved and fixed five observed RED safety regressions. Reports under this directory preserve both failures and subsequent verified results; no review establishes scientific adequacy.

## Required resumption

1. Resolve the denied metadata-only research-artifact access through an explicitly trusted/approved operator execution context; do not bypass the denial or weaken settings automatically. Identify the complete previously tested artifact set, not production customer data.
2. Reconcile prior OpenRouter spend, failed requests and auditor calls. Freeze exact prior Qwen/Gemma identities against official metadata and one cumulative reservation ledger.
3. Complete remaining typed-graph contracts, bounded jobs/stage persistence, frozen evidence/baseline adapters, full per-case acceptance rubric, application entrypoint and Python bridge. Clear all independent spec/quality findings before real calls.
4. Run actual per-role smoke/pilots, then the entire frozen cohort with substantive output audits. Do not relabel contract-only or unavailable stages as completed.

No full-cohort reproduction command exists yet because the actual runner/integration has not been built and the corpus cannot be accessed. The commands above reproduce the exercised software checks only.

Durable state: `/Users/ct-mac-mini/dev/local-model-migration-planning/execution-status.json`.
Per-stage unknown/not-run checklist: `docs/benchmarks/cohort-stage-checkpoint.json`.
Journal: `docs/benchmarks/execution-log.md`.
